/*
 * Para evitar repetição e ser reutilizável,
 * mantemos todas as query de consulta neste objeto.
 */
export const queryKeys = {
    isLogged: ["isLogged"],
    userProfile: ["userProfile"],
};

/*
 * Para evitar repetição e ser reutilizável,
 * mantemos todas as query de consulta neste objeto.
 */
export const mutationKeys = {
    normalLogin: ["normalLogin"],
};
